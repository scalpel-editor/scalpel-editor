# Roadmap

Phases run roughly in order; each lists its deliverable and how it is verified. Update this file when a phase completes or the plan changes.

The phase 1 tests cover the platform-free document and container code well, but they do not instantiate `Editor` or `ScintillaBase`. Because `scintilla_core` is a static library, the test executable also does not link the editor objects it never calls. Phase 2 closes that gap before the dispatch refactor begins. The first visual executable arrives in phase 6; until then, the OnlyWayUi repository (tag `scintilla-seed`) can answer questions about the original core, but it is a reference rather than a regression test for the refactored code.

Phase 2 added the local check matrix. Every later phase must pass it: the normal build, the focused editor tests, and the AddressSanitizer and UndefinedBehaviorSanitizer builds. Development is local; there is no hosted CI. Routine work uses the smallest relevant normal-tree build and focused tests, with the complete normal workflow run before a compiled-code handoff. The three-tree matrix is the phase and release gate, and is also used for changes with broad or sanitizer-sensitive risk. Behavior reductions are deliberate scope changes, not behavior-preserving refactors; document and test the retained behavior in the same change.

Scope principle: the refactor changes how features are reached, not which features exist. Keep Scintilla's editing features intact: the refactored core is only a meaningful artifact if it is recognizably Scintilla — full capability, made legible — rather than a small editor assembled from Scintilla parts. Delete only what serves other platforms or the message layer itself, plus the encoding reduction already decided in phase 3. Feature breadth and compatibility are separate questions: this project owes nothing to message numbers, upstream mergeability, or existing container code; it is the features themselves that are kept. A retained feature with no consumer in this editor yet — autocomplete, call tips, the lexer interface, IME machinery before phase 7 — stays compiled, named, and documented rather than deleted. Styling is expected to come through Lexilla eventually, so the lexer interface must stay compatible with it; wiring Lexilla in and Markdown styling are follow-on work after this roadmap (see "After this roadmap").

## Phase 1 — Stand up the harness ✅ (2026-07-12)

Create CMake for the Scintilla core and the upstream unit tests in `scintilla/test/unit`, and get them green under `ctest`. Document the build and test commands in AGENTS.md in the same change.

Deliverable: `cmake --preset dev && cmake --build build && ctest --test-dir build` works. This is the baseline safety net for the platform-free code; phase 2 extends it through the editor.

Done: the whole core compiles as static library `scintilla_core` (GCC 15, `-Wall -Wextra`, zero warnings); the upstream tests build against it and pass — 3,816 assertions in 52 test cases. `UnitTester.cxx` is excluded from the test build; it is an alternate `main()` for the Visual Studio build, matching the upstream GNU makefile.

## Phase 2 — Test the editor and establish the pattern ✅ (2026-07-13)

Add a concrete test editor and test-only platform implementation that link the whole editor path and expose invalidation, scrollbar, notification, clipboard, and drawing effects for assertions. Trace `SCI_SETWRAPMODE` end to end and dissolve it into a named public method because wrap mode is part of the application's intended surface. Move and condense its `ScintillaDoc.html` prose into a doc comment, leave the switch case as a temporary forwarding call, and test both the state change and its redraw, horizontal-scroll, and scrollbar effects. During the transition, compare the direct method with the forwarding path where that provides a useful mechanical check.

Use this exercise to define a classification applied before converting each later message: application-facing method, private editor operation, keyboard command, retained type or notification, or feature to delete. Under the scope principle, the deletion class covers only platform-only material and the message layer itself. Do not turn every interface entry into a public method. Generate a concern-grouped inventory from `Scintilla.iface`, write a short before-and-after note tracing wrap mode through the message architecture and through the named-method result — the one demonstration write-up this project keeps — and add the local check matrix: `asan` and `ubsan` configure presets alongside `dev`, each in its own build tree, plus a single command that configures, builds, and tests all three. Document the matrix and its phase-gate role in AGENTS.md in the same change.

Deliverable: the full editor path links in a test executable; wrap mode and its side effects have focused tests; the five-way classification and documentation pattern are written down; the wrap-mode before-and-after note exists; the local check matrix runs all three build trees with one command and passes.

Done: `editorTest` links the full `Editor` and `ScintillaBase` path against the deterministic host below. The wrap-mode cases cover the direct method, the temporary forwarding message, visible host effects, and repeated-setting behavior. `MESSAGE_REMOVAL.md` defines the classification and documentation pattern, keeps the wrap trace, and inventories all callable interface entries and notifications by concern. The local `dev`, `asan`, and `ubsan` matrix passes through `check.sh`.

### Concrete test editor contract

"Concrete test editor" means a test-only final subclass of `ScintillaBase` that can be constructed, given a fixed client rectangle, loaded with text, exercised through named editor methods and input methods, and destroyed without Wayland, EGL, OnlyWayUi, or another process. Instantiating it in the unit-test executable must pull `Editor.cxx`, `ScintillaBase.cxx`, and their required core objects through the static-library link, so unresolved editor definitions fail the build.

The fixture owns deterministic test implementations of the current `Platform.h` contracts. Its window stores size, cursor, capture, and invalidated rectangles in memory. Its surface and font return fixed metrics, fill every requested UTF-8 byte position, and record drawing commands instead of producing pixels. Clipboard reads and writes are synchronous in-memory operations. Time advances only when a test requests it. Autocomplete list boxes and call-tip windows are in-memory and inspectable (items, selection, geometry, visibility, delegate events). Menus and other features not under test must either record an explicit request or report that they are unsupported; they must not silently make the observed operation look successful.

Every host callback that can reveal editor behavior has inspectable state: horizontal and vertical scroll updates, scrollbar range and page changes, invalidation, parent notifications with pointed-to text copied before the callback returns, selection ownership, clipboard contents, mouse capture, idle and ticker requests, popup requests, and calls that fall through to `DefWndProc`. Tests can clear this observation state without reconstructing the document, and a compact snapshot supports comparisons between a temporary message-forwarding path and its named replacement.

The first contract tests construct and destroy the fixture under the sanitizers, set and retrieve text, and change the client rectangle. The wrap-mode test starts with a nonzero horizontal offset, enables wrapping, and verifies the new wrap state, zero horizontal offset, redraw request, horizontal-scroll update, and scrollbar reconfiguration. Repeating the same setting must produce no change-only effects. While the forwarding case exists, the same operation through `WndProc` on a fresh fixture must produce the same snapshot as the named method.

This fixture is an editor-state and host-interaction test tool. It is not evidence that glyphs look correct, that Wayland lifecycles are correct, or that asynchronous clipboard and IME behavior works; phase 6 renderer checks and phase 7 shell tests cover those boundaries. The test platform must not become a reason to retain `Platform.h`: when phase 6 installs the concrete renderer, replace the test surface with that renderer's deterministic offscreen path and retain only the test editor's host-observation state.

## Phase 3 — UTF-8 only ✅ (2026-07-13)

Remove DBCS and code-page support before it can shape the new API: delete `DBCS.cxx`, code-page messages and documentation, non-UTF-8 branches in `Document`, `EditView`, case conversion, search, and measurement, and code-page parameters threaded through call chains. Collapse the parallel encoded and UTF-8 surface methods into one UTF-8 path. Decide and test what happens when invalid UTF-8 reaches the editor boundary so the invariant is explicit rather than assumed.

Steps, each landing green through `./check.sh`:

1. Make the invalid-UTF-8 policy explicit and pin it with tests. The policy keeps the current UTF-8-mode behavior: the document stores the bytes it is given without validating or rewriting them; each byte that is not part of a valid UTF-8 sequence acts as a one-byte character for movement, width, and deletion; `GetCharacterAndWidth` reports such a byte as `0xDC80 + byte` (an escape value that is never a real code point); the view draws it as a hex blob. Write the policy as a doc comment on `Document` and add focused document and editor tests: movement and width over invalid bytes, search across them, byte-exact text round-trip through the editor, and case conversion leaving them unchanged.
2. Remove the code-page API: the `SetCodePage` and `GetCodePage` messages with their `ScintillaDoc.html` prose, `Editor::ValidCodePage`, and `Document::SetDBCSCodePage`. `dbcsCodePage` becomes a constant until later steps delete its uses. Delete the DBCS sections of the upstream unit tests in the same change.
3. Dissolve the encoding branches inside `Document` — character iteration, movement, case folding, search, and regular expressions — and delete `DBCS.cxx` and `DBCS.h`. `IDocument::CodePage()` and `IsDBCSLeadByte()` stay with fixed answers because Lexilla lexers call them through the retained lexer interface.
4. Remove code-page values threaded through layout, measurement, and clipboard: `SurfaceMode::codePage` in `Platform.h`, `SpecialRepresentations::SetDefaultRepresentations`, `EditView`, `EditModel`, `CallTip`, `SelectionText`, and the test platform.
5. Collapse the parallel encoded and UTF-8 surface into one UTF-8 path: delete `TargetAsUTF8`, `EncodedFromUTF8`, and `SetLengthForEncode` with the `UTF8FromEncoded`/`EncodedFromUTF8` virtual pair, and fold Unicode case conversion (`CaseConvertString`) into `Editor::CaseMapString`, which was ASCII-only in the base class because real case conversion lived in the deleted platform layers.
6. Sweep the repository for leftover code-page and DBCS references, finish the documentation cleanup, and mark the phase done.

Deliverable: no code path asks which encoding is in use; no API accepts a code page; retained UTF-8 behavior and the invalid-input policy have focused tests; adjusted upstream tests pass.

Done: document movement, search, regular expressions, case conversion, layout, measurement, clipboard text, autocomplete, and drawing now have one UTF-8 path. The parallel encoded surface methods and constant encoding flags are gone, and the seed platform reflects the same interface. `IDocument::CodePage()` and `IsDBCSLeadByte()` remain only because Lexilla requires those methods; they return UTF-8 and false. The generated interface inventory and remaining HTML descriptions match the reduced API, completion searches find no removed encoding path, and the local check matrix passes.

## Phase 4 — Dissolve the dispatch

Apply the phase 2 classification across the `WndProc` switches in `Editor.cxx` and `ScintillaBase.cxx`, grouped by concern. Retained application operations become narrow named methods, internal behavior stays private, key bindings use a dedicated command type, and platform-only material is deleted rather than converted. Deletion candidates are things like technology selection (GDI versus Direct2D) and messages that exist only for other platforms' embedding arrangements; each deletion must state why the thing serves no platform this project has. Features this editor does not use yet — autocomplete, call tips, the lexer surface, printing via `FormatRange` — are retained and converted like everything else. Macro recording is retained with a typed `RecordedAction` variant that owns recorded text and contains a command or another typed editing operation; the numeric message and raw-parameter record format is deleted.

Follow [DISCOVERABILITY.md](DISCOVERABILITY.md) while splitting `Editor.cxx` into files named by what they do (wrapping, caret, scrolling, selection, find, styling, and so on). Keep neighboring code about the same concern, use the same plain feature nouns in filenames and named entry points, and move a complete concern rather than leaving small wrappers in the mixed-purpose file. Keep each change reviewable, add editor tests before moving behavior that is not already covered, migrate concise documentation for retained features, and delete documentation for deleted features in the same change.

Before applying the classification to the full inventory, create the fixed discoverability benchmark described in [DISCOVERABILITY.md](DISCOVERABILITY.md) and record the current-tree baseline. Complete two concern pilots. The first moves the complete wrapping concern, including its named entry points, private work, documentation, tests, and temporary forwarding paths, into a clearly named concern file. The second moves a concern with a different shape; autocomplete and call tips are the preferred candidate because they are owned by `ScintillaBase`, manage popup state, and must remain compiled despite having no initial application consumer. Run vector-only and hybrid grepai searches, whole-repository and source-limited searches, held-out paraphrases, boundary-stability checks, and cold navigation checks before and after each pilot. Apply the larger refactor only after both pilots meet the recorded acceptance criteria without search-engine wording or layout tied to one chunk boundary.

Steps use focused normal-tree tests while iterating and the complete normal workflow before a compiled-code handoff. Step 20 runs the three-tree phase gate:

1. Create the fixed discoverability benchmark. Check in the normal query corpus, held-out queries, expected concern files, entry-point definitions, focused tests, obsolete locations, and a repeatable result format. Record the grepai build and configuration fields needed to reproduce a run. ✅ (2026-07-13, commit `7fe152d`)
2. Record the complete current-tree baseline. Run vector-only and hybrid searches across the whole repository and `scintilla/src`, then record the boundary-stability and cold-navigation checks. Keep the corpus fixed after this point. ✅ (2026-07-13)

Done for step 2: full matrix of 240 searches in `benchmark-results/baseline/` (vector and hybrid × repository and `scintilla/src` × normal and held-out); `summary.json` roll-ups; exact-name `rg` table, boundary-stability re-run, and cold-navigation records in `benchmark-results/baseline/observations.md`. Under the default cell (vector / whole repository), natural-language concern top-3 is about 53% (below the 80% pilot bar), exact-name definition top-3 is 1/12, and obsolete HTML or seed material often outranks live code. Boundary padding before `SetWrapMode` kept wrap concerns in the top three on that default cell. Cold nav reached wrap in two steps (wrong span in the right file first), autocomplete only if the reader continues past helper types, search only if they skip HTML, and undo not within two steps. The corpus is fixed from this point. The baseline run also wiped and re-embedded the whole grepai index; that full rebuild is not a required practice going forward (see `DISCOVERABILITY.md`).

3. Pin the complete wrapping concern with tests. Trace all 11 wrapping interface entries and the private work they reach, classify each entry, and add focused tests for behavior not already observed. ✅ (2026-07-13)

Done for step 3: `MESSAGE_REMOVAL.md` classifies wrap mode as the two application-facing operations and the other nine entries as private editor operations, records the private wrapping work and shared layout boundary, and assigns `LinesJoin` and `LinesSplit` to line transformations. `EditorWrappingTest.cxx` now holds six focused cases with 44 assertions covering all 11 entries: defaults, state changes, redraw and scrollbar effects, repeated-setting behavior, direct versus temporary message forwarding for wrap mode, display-row counts, disabling wrapping, and relayout after resize and text modification. The focused editor executable and local check matrix pass.
4. Complete the wrapping pilot. Move the named operations, private work, documentation, tests, and temporary forwarding paths into clearly named wrapping files, removing obsolete dispatch and documentation as the classification permits. ✅ (2026-07-13)

Done for step 4: the wrapping concern now lives in `scintilla/src/EditorWrapping.cxx` — `Wrapping`, `SetWrapMode`, `GetWrapMode`, the eight visual-flag and indent operations (newly named, since they were previously inline in `WndProc`), `NeedWrapping`, `WrapOneLine`, `WrapBlock`, `WrapLines`, `CheckModificationForWrap`, and `WrapCount`. `LinesJoin` and `LinesSplit` stayed in `Editor.cxx` as line transformations. The `WndProc` cases remain as temporary forwarding calls to the named methods, awaiting the phase 5 generated-layer deletion. The authoritative wrap descriptions moved from the `Editor.h` declaration and the `ScintillaDoc.html` "Line wrapping" section into reader comments beside the definitions; the layout-cache, position-cache, layout-thread, and lines-split/join prose stayed in that HTML section because those concerns have not moved. `EditorWrappingTest.cxx` and the local `dev`/`asan`/`ubsan` matrix pass. The discoverability benchmark re-run is step 5.
5. Measure the wrapping pilot. With the index updated for the moved files, run the full benchmark matrix, held-out queries, boundary check, focused tests, and `./check.sh`. Stop and correct the concern boundary if the recorded acceptance criteria are not met. ✅ (2026-07-14)

Done for step 5: the recorded 240-search matrix is in `benchmark-results/wrapping-pilot/`. Under vector search over the whole repository, all eight normal wrapping queries rank `EditorWrapping.cxx` first, both held-out queries rank in the top two, and the six natural-language queries improve to 100% concern top-three. Hybrid search ranks both exact-name definitions first; exact `rg` checks find the single definitions, declarations, focused tests, and intentional temporary forwarding calls. A roughly 300-character boundary shift keeps all ten default wrapping queries at their original concern ranks; two hybrid whole-repository diagnostic cells move out of the top three. Cold navigation reaches the correct concern file in one search and the public operations in a second exact search. The focused 44 assertions and the local `dev`/`asan`/`ubsan` matrix pass. The runner now takes a verified index snapshot because the live watcher changed `index.gob` during the first attempt and caused an incomplete read. The full corpus also records worse ranks for unmoved `Editor.cxx` concerns after the early wrapping block was removed and all later fixed windows shifted; this is evidence against padding the mixed file to suit the current chunker, not a wrapping-boundary failure. `DISCOVERABILITY.md` records the resulting exact-name criterion clarification.
6. Make popup tests deterministic. Extend the test list box and call-tip host state so tests can inspect items, selection, position, visibility, cancellation, completion, notifications, and explicit unsupported host requests. ✅ (2026-07-14)

Done for step 6: `TestListBox` is a full in-memory list box — it creates a `TestWindow`, stores items (including type separators), selection, geometry, and a delegate, and publishes that state on `TestHost::listBox`. Event injection (`NotifyListBoxSelectionChange` / `NotifyListBoxDoubleClick`) drives `IListBoxDelegate` so completion and selection-change paths run under the test host. `CreateCallTipWindow` assigns `ct.wCallTip` to `TestHost::callTipWindow` and tracks create/show/destroy on `TestHost::callTip`. Menus and bidirectional layout remain request-only. `PopupHostTest.cxx` holds seven smoke cases covering show, cancel (message and CancelModes), double-click completion, call-tip show/cancel, mutual exclusion, and typed list parsing. The focused editor executable and local check matrix pass. Full autocomplete/call-tip coverage and the concern move are step 7.
7. Complete the autocomplete and call-tip pilot. Move both complete concerns into clearly named files, add typed named operations, retain their popup behavior, migrate their documentation, and remove their dispatch cases as the classification permits. ✅ (2026-07-14)

Done for step 7: all 44 autocomplete entries (including user lists and images) and all 11 call tip entries are classified in `MESSAGE_REMOVAL.md`. Named typed operations live on `ScintillaBase` with definitions in `EditorAutocomplete.cxx` and `EditorCallTips.cxx`; private helpers (`AutoCompleteStart`, completion/notify paths, `CallTipShow(Point)`, `CallTipClick`) moved with them. `AutoComplete.cxx` and `CallTip.cxx` remain the lower-level list and tip types. `WndProc` cases are temporary forwarders. Authoritative prose sits beside the definitions; the Autocompletion, User lists, and Call tips sections of `ScintillaDoc.html` are reduced to pointers. `EditorAutocompleteTest.cxx` and `EditorCallTipsTest.cxx` cover show/cancel/complete, option round-trips, choose-single, user lists, stop characters, mutual exclusion, and direct-versus-message parity; `PopupHostTest.cxx` remains the step 6 host smoke suite. Context menu, IME, and lexer paths stay in `ScintillaBase.cxx`. The focused editor executable and local check matrix pass. The discoverability benchmark re-run is step 8.
8. Measure the second pilot and approve the layout rule. Repeat the full benchmark and record any evidence-based acceptance-criteria adjustment in `DISCOVERABILITY.md`. Do not begin the broad split until both pilots pass. ✅ (2026-07-14)

Done for step 8: the recorded 240-search matrix is in `benchmark-results/autocomplete-calltip-pilot/`. Under vector search over the whole repository, all six spaced-name, intent, and effect queries rank the concern file in the top three; both exact queries rank the concern and definition in the top three under vector and hybrid search; and the held-out autocomplete and call-tip queries improve from absent to rank 1 and from rank 9 to rank 6. Independent boundary shifts keep every normal query in the top three without held-out regression, cold navigation reaches each implementation and focused test within two searches, and exact `rg` checks find the intended definitions, declarations, tests, and temporary forwarders. The measurement corrected the source layout before approval: autocomplete named operations now precede its long private workflow, while the public call-tip show operation sits beside its placement helper and directly owns autocomplete cancellation. This rejects a universal public-first rule while approving authoritative documentation beside named definitions and direct ownership of important effects. The focused popup tests and local `dev`/`asan`/`ubsan` matrix pass.
9. Classify the remaining inventory. For every entry in `MESSAGE_REMOVAL.md`, record its owner, classification, target file, named entry point, focused test, documentation action, and deletion reason where applicable. ✅ (2026-07-15)

Done for step 9: the exhaustive 821 callable entries and 32 notifications now have concern decision tables recording ownership, the five-way classification, target files and typed operations, focused-test work, documentation action, and concrete reasons for every deletion. `tools/check-message-inventory.sh` verifies that the inventory contains every current `Scintilla.iface` callable and notification exactly once. Application methods are limited to the standalone editor's file, basic selection, search, history, scrolling, focus, lexer attachment, and recording needs; unused editing features remain compiled as private typed concerns. Direct-call accessors, shared-document handles, generated structure aliases, renderer-selection controls, deprecated compatibility entries, widget identifiers, and host behavior with no Wayland implementation are scheduled for deletion. Macro recording is retained through an owned typed `RecordedAction` rather than message numbers and raw parameters.
10. Introduce the dedicated command type. Replace `Message` in key maps, keyboard dispatch, movement helpers, popup commands, and internal calls through `WndProc`, with representative tests for movement, selection extension, editing, clipboard, undo, and redo. ✅ (2026-07-15)

Done for step 10: `EditorCommand` in `EditorCommands.h` is the bindable-action type (`None` for unbound). `KeyMap` stores `EditorCommand`; `KeyDownWithModifiers` looks up a binding and calls `ExecuteCommand` without going through `WndProc`. `Editor::ExecuteCommand` in `EditorCommands.cxx` runs the former `KeyCommand` body plus zero-argument clipboard, history, selection, line, and zoom-reset actions. Movement helpers (`PositionMove`, `HorizontalMove`, `DelWordOrLine`, and the extend helpers) take `EditorCommand`. `ScintillaBase::ExecuteCommand` keeps autocomplete and call-tip interception. Temporary `WndProc` cases and `AssignCmdKey` still accept recognized message numbers through `CommandFromMessage` until phase 5; unsupported assignments leave the existing binding unchanged. Keyboard execution temporarily uses `MessageFromCommand` to preserve numeric macro notifications until step 12 installs the typed recorder. Parameterized search (`SearchNext`/`SearchPrev` with text) stays on the message path. `EditorCommandTest.cxx` covers movement, extension, editing, clipboard, undo/redo, key bindings, reassignment, macro recording, unsupported assignment, and direct-versus-message parity. The focused editor executable and local check matrix pass.
11. Migrate the remaining concerns one at a time. Work through document and history, clipboard, lines and EOL, selection and caret, search, scrolling and view, margins and folding, styling, markers and annotations, input, lexing, and printing. Add tests before moving unobserved behavior, move each complete concern, migrate or delete its documentation, replace retained dispatch bodies with thin temporary forwarders, delete cases for removed features, and run the complete normal workflow before handing off each compiled-code change.

Progress: document and text moved to `EditorDocument.cxx` with `EditorDocumentTest.cxx` (2026-07-15). Application methods: `SetText`, `GetText`, `GetTextLength`, `GetModify`, `SetReadOnly`, `GetReadOnly`. Private mutators and range reads live in the same file. Document-sharing messages (`GetDocPointer` / `SetDocPointer` / `CreateDocument` / ref-count / `CreateLoader`) and the `*Full` range aliases are deleted from dispatch. Undo and history moved to `EditorHistory.cxx` with `EditorHistoryTest.cxx` (2026-07-15): `CanUndo` / `CanRedo` / `SetSavePoint` / undo groups as application methods; `Undo` / `Redo` and buffer control as commands and private operations. Clipboard moved to `EditorClipboard.cxx` with `EditorClipboardTest.cxx` (2026-07-15): `CanPaste`, cut/copy-allow-line, range and text copy, multi-paste and ending-conversion options; `Copy`/`Paste` remain host hooks. Lines, whitespace, and EOL moved to `EditorLines.cxx` with `EditorLinesTest.cxx` (2026-07-15): `SetEOLMode` / `GetEOLMode` / `ConvertEOLs` as application methods; line queries, indent/tab settings, character classes, `LinesJoin` / `LinesSplit`, and related private ops; line-state messages remain until lexing. Selection moved to `EditorSelection.cxx` with `EditorSelectionTest.cxx` (2026-07-15): basic caret/anchor/range APIs and `GotoLine` / `GotoPos` as application methods; multi-selection, rectangular, hide, and serialization as private ops; selection commands use helpers in the same file. Caret moved to `EditorCaret.cxx` with `EditorCaretTest.cxx` (2026-07-15): appearance, blink period, sticky, caret line, policies, and `VerticalCentreCaret`. Search and replace moved to `EditorSearch.cxx` with `EditorSearchTest.cxx` (2026-07-15): target range, `SearchInTarget`, replace variants, search flags, `ReplaceSel`; `FindText` / `FindTextFull` deleted from dispatch. Scrolling moved to `EditorScrolling.cxx` with `EditorScrollingTest.cxx` (2026-07-15): first visible line, x-offset, line/vertical scroll, scrollbar and width options. Margins moved to `EditorMargins.cxx` with `EditorMarginsTest.cxx` (2026-07-15): margin count, type, width, mask, sensitivity, cursor, back colour, left/right gaps, fold-margin colours, margin text/styles/offset/options. Folding moved to `EditorFolding.cxx` with `EditorFoldingTest.cxx` (2026-07-15): fold levels, expand/toggle/fold-all, automatic/flags/display text, show/hide lines, ensure-visible, last-child/parent, and doc↔display mapping. Styling moved to `EditorStyling.cxx` with `EditorStylingTest.cxx` (2026-07-15): style definition, document style bytes, elements, whitespace view, selection colours, layout caches, phases, zoom, long-line edges, idle styling, extended styles, and TextWidth; deleted buffered-draw/technology/font-quality/scale-technique and two-phase/style-bits aliases. Temporary message forwarders remain for retained operations. Documentation for the moved concerns sits beside the named definitions; matching `ScintillaDoc.html` API prose is reduced to pointers. Markers moved to `EditorMarkers.cxx` with `EditorMarkersTest.cxx` (2026-07-15): all 26 definition, placement, handle, and search operations as private named methods; temporary message forwarders retained. Decorations moved to `EditorDecorations.cxx` with `EditorDecorationsTest.cxx` (2026-07-15): indicators, braces, hotspots, representations, annotations, and EOL annotations as private named methods; find-indicator animation messages deleted; temporary message forwarders retained. Input moved to `EditorInput.cxx` with `EditorInputTest.cxx` (2026-07-15): focus, IME, dwell, capture, cursor, drag-drop, `ChangeInsertion`, and `UsePopUp`; overtype and key-map configuration in `EditorCommands.cxx`; accessibility, `GrabFocus`, and KeysUnicode deleted from dispatch; temporary message forwarders retained. Text boundaries moved to `EditorTextBoundaries.cxx` with `EditorTextBoundariesTest.cxx` (2026-07-15): position before/after/relative, word extents, char classes, character pointer, cur-line, line character-index mapping; `GetTag` to `EditorSearch.cxx`; virtual-space options to `EditorSelection.cxx`; temporary message forwarders retained. Lexing moved to `EditorLexing.cxx` with `EditorLexingTest.cxx` (2026-07-15): `LexState`, `SetILexer` / `Colourise` / properties / keywords, substyles and named styles, line state and `ChangeLexerState`; temporary message forwarders retained. Printing moved to `EditorPrinting.cxx` with `EditorPrintingTest.cxx` (2026-07-15): print magnification/colour/wrap and typed `FormatRange`; `FormatRangeFull` deleted from dispatch; temporary message forwarders retained. Step 11 concern moves are complete; temporary `WndProc` forwarders remain until phase 5. Steps 12–18 are complete. Next: step 19 (documentation and discoverability cleanup).

12. Move the fixed-host settings. Put modification-event filtering, command-event filtering, status reporting, and fixed feature queries in `EditorHost.cxx` as typed private operations. Add `EditorHostTest.cxx` coverage for notification filtering, change notifications, error status, and every fixed feature answer. Move their useful documentation beside the definitions and leave only temporary `WndProc` forwarders. ✅ (2026-07-15)

Done for step 12: `EditorHost.cxx` owns typed modification-mask, command-event, status, and surface-feature operations plus the document error callback. The temporary `WndProc` cases only unpack parameters and forward. `errorStatus` is no longer exposed through the old COM-oriented public field. `EditorHostTest.cxx` covers defaults, direct and message round-trips, detailed-notification filtering, independent coarse change-event filtering, status clearing, and all six deterministic test-surface feature answers. The useful API prose moved beside the definitions and the matching HTML sections now point there. Feature support still comes from the active surface; Phase 6 will supply the fixed Wayland renderer answers. The inventory verifier, durable grepai index check, focused editor suite, and local `dev`/`asan`/`ubsan` matrix pass.
13. Delete the remaining host and embedding operations. Remove the direct-call accessors, widget identifier, and any renderer-selection or host operation still present after the concern moves. For each deletion, update `MESSAGE_REMOVAL.md`, delete its `ScintillaDoc.html` prose, and add or retain a focused test showing the standalone host reaches the needed behavior directly or intentionally does not provide it. ✅ (2026-07-15)

Done for step 13: `SetIdentifier` / `GetIdentifier` and the `ctrlID` / `SetCtrlID` / `GetCtrlID` plumbing are gone from dispatch and `Editor`. `GetDirectFunction` / `GetDirectStatusFunction` / `GetDirectPointer` never had core cases; their Direct-access HTML prose is removed and fall-through tests pin the absence. Renderer-selection messages were already deleted with the styling move. `EditorHostTest.cxx` covers identifier and direct-accessor fall-through plus single-host notifications without a widget id. Matching inventory rows and HTML sections record the deletions. Generated `Message` constants remain until phase 5. Recording remains for steps 14-16.
14. Define the typed recording contract. Add `EditorRecording.h` with an owned `RecordedAction` variant for `EditorCommand` and each parameterized recordable edit, plus the typed callback used by the fixed host. Spell out which operations are recordable, which display-only operations are ignored, how inserted text is owned, and how replay avoids recording itself. Extend the test host to retain recorded actions after the callback returns. ✅ (2026-07-15)

Done for step 14: `EditorRecording.h` defines `RecordedAction` (command, replace-selection, add/insert/append text, clear-all, goto line/pos, search anchor, parameterized search, selection mode) and `RecordingCallback`. `IsRecordableCommand` defines the exact zero-argument allowlist, and `RecordedCommand` rejects search, undo/redo, newline, zoom, and every command outside that list; search anchor and parameterized search therefore use only their dedicated alternatives. The header documents recordable versus ignored operations, UTF-8 and invalid-byte text ownership, and the no-recursive-recording rule for replay. `TestEditor` retains a full copy of each action in `observations.recordedActions` after the callback returns. `EditorRecordingTest.cxx` covers every alternative, rejected command forms, multi-byte and invalid UTF-8 lifetime after the original is mutated or dropped, and clear/empty host behaviour. Production still records through `NotifyMacroRecord` / `SCN_MACRORECORD`; lifecycle and capture moves are steps 15–16. Inventory and HTML point at the typed contract. The focused editor suite and local check matrix pass.
15. Move recording lifecycle and command recording. Add `EditorRecording.cxx` with `StartRecording()`, `StopRecording()`, recording-state handling, command capture, and replay. Record commands at `ExecuteCommand` rather than converting them back through `MessageFromCommand`; test start/stop boundaries, representative movement, selection, editing, clipboard, undo/redo, replay order, and the no-recursive-recording rule. ✅ (2026-07-15)

Done for step 15: `EditorRecording.cxx` owns lifecycle (`StartRecording` / `StopRecording` / `IsRecording`), `SetRecordingCallback`, `EmitRecordedAction`, and replay (`ReplayRecordedAction` / `ReplayRecordedActions` with a replaying guard). `ExecuteCommand` emits `RecordedCommand` for every `IsRecordableCommand` value; `KeyDownWithModifiers` no longer converts through `MessageFromCommand` for capture. The temporary `NotifyMacroRecord` allowlist is limited to parameterized messages; zero-arg commands no longer emit `SCN_MACRORECORD`. Autocomplete-consumed keys never reach `Editor::ExecuteCommand` and are not recorded. Temporary `StartRecord` / `StopRecord` message cases only forward. `TestEditor` installs the typed sink in its constructor. `EditorRecordingTest.cxx` covers start/stop, nested start, message parity, representative command capture, non-recordable commands (undo/redo/newline/zoom/search), message-versus-direct agreement, replay order, and no recursive recording. The focused editor suite and local check matrix pass. Parameterized capture and numeric-path deletion remain step 16.
16. Convert parameterized recording operations. Record inserted or replaced text, navigation positions, selection mode, and parameterized search at their typed named entry points. Test every `RecordedAction` alternative, byte-exact UTF-8 and invalid-byte text ownership after callback return, ignored tentative input, newline behavior, and replay on a fresh editor. Delete `NotifyMacroRecord`, the catch-all recording hook at the start of `WndProc`, numeric macro notification fields, and the `MessageFromCommand` bridge. ✅ (2026-07-15)

Done for step 16: parameterized ops emit typed `RecordedAction` at named entry points — document text in `EditorDocument.cxx` / `EditorSearch.cxx`, goto and selection mode in `EditorSelection.cxx`, search in `EditorSearch.cxx`, character insert and newline as `RecordedReplaceSelection` (tentative IME skipped). `NotifyMacroRecord`, the `WndProc` recording front-hook, and `MessageFromCommand` are deleted. Production never emits `SCN_MACRORECORD`; generated notification constants remain until phase 5. `EditorRecordingTest.cxx` covers every alternative (production capture, ownership, tentative, newline, mixed and constructed replay on a fresh editor, message-versus-named parity). The focused editor suite and local check matrix pass.
17. Audit every retained entry point. Use the checked-in inventory verifier and repository searches to confirm that each retained interface entry has one typed named operation, a concern file, nearby documentation, and focused coverage; a temporary message case may only unpack parameters and immediately call that operation. Confirm that every deletion in the ledger has no dispatch case and no live API prose. ✅ (2026-07-16)

Done for step 17: `tools/check-retained-entrypoints.sh` (and the inventory verifier) confirm every retained `fun`/`get`/`set` has a thin temporary `Message::` case and a named method or `EditorCommand`, with intentional renames listed as aliases; every `Feature to delete` has no dispatch case. Gaps closed in the same step: six typed `SetSelectionN*` setters (replacing `SetSelectionNMessage`) and `SetRectangularSelectionModifier` / `GetRectangularSelectionModifier` (default Alt, used for mouse rectangular drag). Application-facing search, selection, and scroll ops gained direct-path assertions where they had been message-only; `SCN_KEY` and `SCN_URIDROPPED` live HTML prose was replaced with deletion notes. Spot-checked application definitions keep reader comments beside the implementations. Temporary `WndProc` shells remain for phase 5.
18. Audit the concern split. Review the functions still in `Editor.cxx` and `ScintillaBase.cxx`; move any complete feature concern that remains, but keep genuinely shared paint, geometry, notification, document-watcher, and dispatch-shell work in place for their later phases. Confirm that tests no longer use a message call as the only proof of application-facing methods and commands, and that no internal production call reaches behavior through `WndProc`. Private operations may remain covered through their thin compatibility cases until phase 5 deletes those cases. ✅ (2026-07-16)

Done for step 18: the remaining-layout map in `MESSAGE_REMOVAL.md` records that `Editor.cxx` keeps shared paint, geometry, scroll infrastructure, selection primitives, core edit primitives, movement helpers, notifications, mouse/idle, and the temporary dispatch shell, and that `ScintillaBase.cxx` keeps the subclass shell (lifecycle, popup command routing, IME helpers, temporary forwarders). No further concern files were added. Production `WndProc` use in `ScintillaBase::Command` and `ContextMenu` is gone — menu actions call `ExecuteCommand`, enablement uses `GetReadOnly` and `CanPaste`. `PopupHostTest` covers menu enablement and each menu command. Application-facing suites exercise named methods or `RunCommand` as primary proof (message parity remains optional). Inventory verifiers stay green. Next: step 19 (documentation and discoverability cleanup).
19. Finish documentation and discoverability cleanup. Remove obsolete `ScintillaDoc.html` prose for every moved or deleted feature, update `MESSAGE_REMOVAL.md` from planned to completed state, and update this roadmap, `AGENTS.md`, and other project text that still describes `Editor.cxx` as one file. Update grepai for every moved source file, then run the exact-name, cold-navigation, held-out, and boundary-stability checks without changing the fixed corpus.
20. Run the phase gate. Run the complete discoverability benchmark, the inventory verifier, completion searches for internal `WndProc` calls and application-facing behavior tested only by message, all focused editor tests, and `./check.sh`. Record the final benchmark and mark Phase 4 complete only when the remaining `WndProc` switches are thin compatibility forwarders whose deletion, together with the generated message constants and parameter helpers, is wholly Phase 5 work.

Deliverable: the two pilots and the full concern inventory meet the discoverability checks in [DISCOVERABILITY.md](DISCOVERABILITY.md); no retained behavior is reachable only through a message number; deleted behavior and its documentation are gone; `Editor.cxx` no longer exists as a 9,000-line file; focused tests cover each moved concern.

## Phase 5 — Delete the generated message layer

Remove the remains of `WndProc`, `DefWndProc`, numeric message constants, parameter coercion helpers, `ScintillaCall.h`, `ScintillaMessages.h`, the C message surface in `Scintilla.h`, and `Scintilla.iface`. Before deleting the generator, separate the enums and structures the core still needs from generated client API material: replace retained parts of `ScintillaTypes.h` and `ScintillaStructures.h` with small project-owned definitions, remove the generated `MacroRecord` notification entry now replaced by the Phase 4 typed recording callback, and remove unrelated public headers such as the GTK-only `ScintillaWidget.h` when nothing uses them.

Delete `ScintillaDoc.html` when every retained feature has a nearby doc comment and every deleted feature's prose is gone. Completion searches must find no message-number dispatch, `Message` command type, `SCI_*` command constants, generated-section markers, or client parameter-packing helpers. Keep the editor link test so a static archive cannot hide missing definitions.

Deliverable: the name-to-number-to-switch path and its generated client surface are gone; retained types have one project-owned definition; repository-wide completion searches and all tests pass.

## Phase 6 — Renderer and minimal Wayland vertical slice

Build the renderer together with the smallest shell needed to verify it: a Wayland connection, xdg-toplevel, EGL context and surface, event loop, resize and focus handling, basic keyboard and pointer input, timers, and a visible editor buffer. This is the first runnable program in the repository. The renderer and shell are also meant to be read: they are this project's worked example of a C++ Wayland application without a GUI toolkit, so favor clear structure and comments that explain protocol decisions, and keep them an example to read, not a library to import. Keep a deterministic offscreen or image-comparison path for renderer tests so drawing is checked without relying only on manual inspection, and use a checked-in test font with a compatible license so system font changes cannot move expected pixels or caret positions.

Collapse `Platform.h` into the one concrete implementation, mining `seed/editor/PlatOWUI.cxx` and `seed/backends/OnlyWayUi_Renderer_GL3.cpp` for working knowledge. Use FreeType for font metrics and glyph rasterization, HarfBuzz for shaping, and Fontconfig to find system fonts and fallbacks. Shape text once into a cached run containing glyphs, advances, offsets, input-byte clusters, and valid caret stops; measurement, wrapping, hit testing, selection, and drawing must all consume that same result. Correct left-to-right layout with required shaping is the goal; disable discretionary ligatures. Bidirectional screen-line layout is not on this roadmap (HarfBuzz shapes directional runs but does not order a mixed-direction line, and that ordering step is a project of its own). Leave the door open for it: keep the shaped-run cache carrying cluster and direction information, and do not collapse the run model into per-character assumptions that reordering would have to undo.

`ListBox`, `Menu`, and the call-tip window serve autocomplete, call tips, and the context menu, none of which a plain text editor exercises yet. Keep their core-side code compiled and retained under the scope principle; the Wayland platform gives them explicit not-yet-implemented stubs that record the request or fail loudly, never fake success. Implementing real popup windows is follow-on work. Delete the absorbed `PlatOWUI` and renderer seed files while retaining the backend references still needed by phase 7, and preserve the OnlyWayUi license notice on derived code.

Deliverable: a minimal editor runs in a Wayland window and draws through one renderer with no product abstraction; measured byte positions, caret stops, selection bounds, and rendered glyph positions agree for ASCII, kerning pairs, combining sequences, required shaping, and font fallback; automated image checks cover clipping, text, caret, selection, and scrolling.

## Phase 7 — Complete the Wayland shell

Finish the window and input layer by mining the parts that actually exist in `seed/backends/`: compositor and seat lifecycle, xdg-toplevel and decoration handling, cursor themes, EGL damage, clipboard transfers, presentation feedback, and portal event-loop support. Pace drawing with `wl_surface.frame`; use presentation-time only to learn when a submitted frame was displayed.

Implement the features the seed does not contain from the relevant protocol and library sources: xkbcommon compose and key repeat, text-input-v3 for IME, primary selection, fractional scaling and viewporter support, output and buffer-scale changes, and their fallback behavior when an optional protocol is unavailable. Keep pointer, caret, font, cursor, damage, and buffer coordinates consistent across scale changes. Absorb the asynchronous D-Bus watches, timers, and xdg-foreign parent handle needed by the phase 8 file chooser without putting file-dialog policy in the shell.

Deliverable: keyboard compose and repeat, IME pre-edit and commit, pointer input, cursors, clipboard, primary selection, focus and seat changes, scaling, resize, frame callbacks, and presentation feedback work in the standalone editor; protocol-unavailable paths are tested; all absorbed backend seed files are deleted with required license notices retained.

## Phase 8 — Chrome and a shippable plain text editor

Bespoke, compile-time-known chrome: scrollbar, find bar, and only the other controls that earn their place. Add asynchronous open and save dialogs through the desktop portal and the small application-facing editor methods those features actually need. This phase ships a plain text editor; styling and Markdown come after it is solid (see "After this roadmap").

Define "daily usable" with observable behavior: dirty-buffer prompts on close and replacement, atomic saves with explicit symlink and permission handling, external-file change detection and conflict handling, a tested invalid-UTF-8 file policy, preserved or deliberately normalized line endings, clear portal cancellation and failure behavior, and a documented response to files too large for comfortable editing. Test open, edit, save, save-as, failed save, external change, and close-with-unsaved-work flows.

Deliverable: daily-usable plain text editing that satisfies the file-lifecycle checks above, with application workflow tests and no generic UI framework.

## After this roadmap

Work the plan deliberately leaves out but keeps the door open for, in rough order of expected value:

- Lexilla integration and Markdown-aware styling, once the plain text editor is solid. The retained lexer interface is the attachment point.
- Wayland popup windows for autocomplete, call tips, and the context menu, replacing the phase 6 stubs. The core-side feature code is already retained and converted.
- Bidirectional screen-line layout, building on the direction-aware shaped-run cache from phase 6.
